'use strict';

angular.module('myApp.user')
.factory('myApp.user.favoritesService', [
function() {
  var service = {};

  service.listAllFavorites = function(userId) {
    //
  }

  service.like = function(data) {
    //
  };

  service.unlike = function(data) {
    //
  };

  return service;
}]);